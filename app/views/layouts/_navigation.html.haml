= link_to "Liautho", root_path, :class => 'brand'
.auth.span2.offset8
  - if current_user
    = "Signed in as #{current_user.name}"
    = link_to "Sign out", signout_path
  - else
    = link_to "Sign in with LinkedIn", "/auth/linkedin"
